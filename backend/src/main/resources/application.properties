logging.level.ROOT=INFO

spring.datasource.url=jdbc:mysql://localhost:3306/application?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=database
spring.datasource.password=GVpucrE7n4uAEjMspDTA
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
# Possible values: create, create-drop, update, validate
# Should use validate in production
spring.jpa.hibernate.ddl-auto=update
# Allow use of ** and pattern matching afterwards
# Used in FrontendController
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# Database initialisation
spring.sql.init.mode=always
spring.sql.init.continue-on-error=false